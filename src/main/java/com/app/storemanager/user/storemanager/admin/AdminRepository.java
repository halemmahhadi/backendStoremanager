package com.app.storemanager.user.storemanager.admin;

import com.app.storemanager.user.storemanager.StoreManagerRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface AdminRepository extends StoreManagerRepository {
}
